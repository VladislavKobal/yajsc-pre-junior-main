name: Week 3 - Task 10

run-name: Running test for week 3 and task 10

on:
  pull_request:
    branches:
      - main
    paths:
      - 'src/tests/week3_tasks/10*.js'
jobs:
  week3-task10:
    uses: ./.github/workflows/reusable.yml
    with:
      npm-command: 'test-10'
